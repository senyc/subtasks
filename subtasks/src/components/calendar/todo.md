1. make modal to the left/right and allow dragging etc while open and it follows (like gcal) this should be a popover not modal
2. add time edit fields and goal/project selection for tasks
3. add complete option for tasks
4. add overlapping support
5. add projects/goals to header (just have separate json blob of these e.g. header/body:
6. allow editing of projects/goal modal
7. add full day events (with db column e.g. whole_day)
8. add the whole_day to be used anywhere for event modal (but not tasks)
9. allow these to be added by clicking header
10. allow full day events to be added by dragging like side-to-side
11. cleanup
12. add month view
13. cleanup
14. add recurring support (basic, so set recurring tasks/events be able to delete specific ones, but not split them up e.g. diverged from recurring parent)
15. cleanup
16. rename goals etc remove tags
17. add dynamic task addition
18. add lists

add ability to color events
be able to swap an event to task and vice-versa
